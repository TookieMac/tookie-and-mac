package application.frontEnd;

import javafx.scene.Scene;
import javafx.stage.Stage;

public class LibraryGUI extends GUIBase{

	
	public LibraryGUI(Stage primaryStage, Scene previousScene) {
		super(primaryStage, previousScene); //initialises in the base class

	}
}
